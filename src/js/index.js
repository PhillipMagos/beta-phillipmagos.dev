// Initialization for ES Users // this is for the carousel
import {
    Collapse,
    Dropdown,
    Ripple,
    Carousel,
    initTE
} from 'tw-elements';

initTE({ Collapse, Dropdown, Ripple, Carousel });